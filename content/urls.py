from django.urls import path

# from .views import (
#     AnswerQuestionView,
#     CompleteContentView,
#     ContentDetailView,
#     ContentView,
# )

urlpatterns = [
    # path("", ContentView.as_view(), name="create-get-list"),
    # # path("create/", InitiateContentCreationView.as_view(), name="create-content"),
    # path("<str:id>/", ContentDetailView.as_view(), name="update-get-detail"),
    # path("<str:id>/complete/", CompleteContentView.as_view(), name="complete-content"),
    # path(
    #     "<str:id>/answer/",
    #     AnswerQuestionView.as_view(),
    #     name="answer-question",
    # ),
]
